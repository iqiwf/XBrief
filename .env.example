# Server-only. Never put a real key in the browser or commit one.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash

# Optional. Signs regenerate drafts. If empty, the API key is used.
SESSION_SECRET=

# Local server only. Vercel ignores these.
HOST=127.0.0.1
PORT=3000
