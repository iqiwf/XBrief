# Server-only. Never put a real key in the browser or commit one.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash

# Local by default so the key is not exposed on your network.
HOST=127.0.0.1
PORT=3000
